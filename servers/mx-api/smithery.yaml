startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |
    (config) => ({
      command: "node",
      args: ["build/mx-api-index.js"],
      env: {}
    })
